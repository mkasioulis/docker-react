sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t mkasioulis/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true mkasioulis/docker-react npm run test
